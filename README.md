# 📋 ToDo App (React + LocalStorage)

Простое ToDo-приложение, созданное на **React** с использованием функциональных компонентов, хуков **useState** и **useEffect**.  
Приложение позволяет добавлять задачи, удалять их, очищать весь список и сохранять данные в **localStorage**, чтобы задачи не исчезали при перезагрузке страницы.

---

## 🚀 Функциональность

- ✨ Добавление задач  
- 🗑 Удаление отдельной задачи  
- 🔥 Кнопка «Очистить всё»  
- 💾 Автосохранение данных в localStorage  
- ⚛️ Компонентная структура:
  - App
  - AddTaskForm
  - ToDoList
  - ToDoItem

---

## 📂 Структура проекта

    src/
    ├── App.jsx
    ├── AddTaskForm.jsx
    ├── ToDoList.jsx
    └── ToDoItem.jsx

## 🛠 Технологии

- React (хуки)
- JavaScript (ES6+)
- LocalStorage
- Vite

---

## 📦 Установка и запуск

1. Клонировать репозиторий:
   ```bash
   git clone https://github.com/your-repo/todo-app.git
  Перейти в папку проекта
  
    cd todo-app
Установить зависимости:

    npm install
Запустить проект:

    npm run dev

  ##  🧩 Использование
Введите текст задачи и нажмите Добавить\
Удаляйте задачи кнопкой Удалить\
Очищайте весь список кнопкой Очистить всё\
Все данные сохраняются в localStorage
Лабораторная работа №8. Введение в React + JSX

# Лабораторная работа №8. Введение в React + JSX
<img width="1920" height="960" alt="image" src="https://github.com/user-attachments/assets/f4a44f36-a2f8-4350-a53d-1d44f63557a1" />
## 📌 Что такое компонент, пропсы и состояние

### Компонент — это независимая часть интерфейса, которую можно многократно использовать. Он инкапсулирует логику и внешний вид, принимает данные через пропсы и возвращает JSX для отображения. Пример компонента: кнопка, форма или список задач. Компоненты помогают делить интерфейс на логические блоки, переиспользовать один и тот же код и упрощают поддержку проекта.

### Пропсы (props) — это данные, которые передаются компоненту снаружи. Компонент может их использовать, но не изменяет напрямую. Пропсы позволяют передавать данные и функции между компонентами, делая их гибкими и переиспользуемыми. Пример: список задач получает массив задач и функцию удаления через пропсы.

### Состояние (state) — это внутренние данные компонента, которые он может изменять. Изменение состояния автоматически вызывает перерисовку компонента. State используется для динамических данных, например, списка задач, формы ввода или счетчика. Состояние позволяет компоненту быть интерактивным и реагировать на действия пользователя.

### Я понял, что компонентный подход позволяет делить приложение на маленькие независимые блоки, каждый компонент решает свою задачу, компоненты можно переиспользовать через пропсы, а управление данными внутри компонента осуществляется через state. Изменение состояния автоматически обновляет интерфейс. Такой подход делает код структурированным, модульным и удобным для расширения.



   
